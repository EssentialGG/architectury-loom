pluginManagement {
	plugins {
		id 'gg.essential.loom-repositories'
	}
}

plugins {
	id 'gg.essential.loom-repositories'
}

dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
}

include 'basic'
include 'projmap'